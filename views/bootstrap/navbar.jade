nav.navbar.navbar-default
  .container-fluid
    .navbar-header
      button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar-collapse', aria-expanded='false')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar

      a.navbar-brand(href='/')
        span.icon-pin Biscornu

    .navbar-collapse.collapse#navbar-collapse
      
      form.navbar-form.navbar-right(role='search')
        .form-group
          input#channel.typeahead.form-control(type='text', placeholder='Slack Channel')

      ul.nav.navbar-nav.navbar-right
        li
          a.icon-up-circled(href='/topvoted') Top Voted
        li
          a.icon-shuffle(href='/random') Random
        li
          if loggedIn
            a(href='#')
              img(src=user.avatar)
              span= ' @' + user.name
          else
            a.icon-slack(href='https://slack.com/oauth/authorize?scope=identity.basic&client_id=' + clientId) Sign in
